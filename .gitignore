# Dependencies
node_modules/

#designs
designs/

# Build output
dist/
out/
desktop/out/
desktop/dist/
# 打包前 beforeBuild 下载的内置 Python / Node(体积大、按平台/arch 拉取,不入库)
desktop/build/python/
desktop/build/node/
# 从 node_modules 拷出的白板引擎自托管副本(引擎 6.3M + 字体 13M,可再生,不入库)
# desktop=postinstall 生成;web=`npm run prepare-board` 生成(build/dev 前置)
desktop/frontend/public/excalidraw/
web/public/excalidraw/

# TypeScript
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# 插件 drop-in 目录：运行时从 ./plugins 发现加载（见 src/tangu-agent/plugins/loader.ts）。
# worker 等插件独立维护/分发，永不提交进核心仓库（开源核心 = standalone/desktop/TUI）。
/tangu-agent/plugins/
