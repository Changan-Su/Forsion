# Dockerfile 专属 ignore(BuildKit:优先于根 .dockerignore 且整体替换它)。
# 根 .dockerignore 排除了整个 `desktop`,但 web 经别名复用 desktop/frontend/src,
# 故这里放行 desktop/frontend,只剔除构建产物/依赖与无关大目录。
**/node_modules
**/dist
**/.vite
**/out
.git
*.log
.env
.env.*
# 构建只 COPY desktop/frontend + web;以下大目录即便不 COPY 也排除以缩小上下文。
archived
designs
desktop/electron
desktop/build
# ⚠️ 例外:白板引擎的自托管副本由这支脚本在镜像里现生成(web 的 build 前置 prepare-board 调它)。
#    上面那条 `desktop/build` 会把它一起排掉 → COPY 直接报 not found,镜像构建当场失败。
!desktop/build/copy-excalidraw-assets.cjs
#    同理:UI 字体预设的裁剪脚本(web 的 prepare-fonts 调它)。
!desktop/build/gen-fonts.cjs
desktop/out
