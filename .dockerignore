# Tangu worker 镜像构建上下文裁剪（context = Forsion-Genesis 仓根;Dockerfile.standalone 只 COPY tangu-agent/）
node_modules
dist
desktop
web
mobile
lcl
archived
# 插件 drop-in 目录（worker 等）：运行时加载，不烘焙进核心镜像。
.git
# 嵌套插件仓（forsion-worker 独立成仓）的 git 历史别烘进镜像（裸 .git 只匹配根级）
tangu-agent/plugins/*/.git
*.log
.env
.env.*

#designs
designs/